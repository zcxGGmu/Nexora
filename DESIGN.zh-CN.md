> 中文实现契约，英文 `DESIGN.md` 为术语对照。

# Nexora 设计系统

## 0. 研究日志

- 嵌入式参考：筛选了 `linear.app.md`、`sentry.md` 和 `aside.md`；采用 `taste-skill.md` 的运营纪律，以及 `linear.app.md` 密集、克制的应用外壳语法。它们是参考材料，不是复制品牌名称、文案或标志性色彩的要求。
- 产品证据：[Agent OS 设计](docs/agent-os-design.md)提供控制平面状态、视觉资产、角色和安全约束。[AionUI 对比](docs/aionui-analysis-and-agent-os-comparison.md)提供工作区绑定的 Cowork、Team、Preview、Cron 以及运行时确认模式。
- UI 研究：已针对运营型 Agent OS 运行 `ui-ux-pro-max` 设计系统查询。其活动落地页建议已被否决，因为活动布局不适合长期运行的控制台语法。
- 查看过的视觉参考：[联系表](docs/assets/agent-os-frontend/contact-sheet.png)，仅作为结构来源。它验证了密集分诊的必要性，而不是固定的像素布局。
- 来源边界：本环境无法访问所提供的 `qMvkdMzuYjs` YouTube 页面。以下 Agent OS 主题取自仓库中受证据边界约束的来源综合；任何未经验证的视频序列、字幕、性能、成本或安全声明都不作为事实采用。
- 延后事项：由于本仓库目前是文档规范而非可运行 UI，尚未运行图像生成或视觉 QA。在进行 C10 屏幕编排之前，实现第 5 节所述的基础组件展示（Primitive Showcase），并在定义的断点运行视觉 QA。

## 1. 氛围与身份

Nexora 是一个用于重要 Agent 工作的安静操作室：足够紧凑，便于持续扫描；足够沉着，支持判断；并明确说明系统知道什么、做了什么、还在等待什么。它的标志是**证据主线（evidence spine）**：操作员无需改变心智模型，就能从人类可读的请求追溯到确切的 Goal、Ticket、Run、Artifact 版本、审核决定和回执。界面采用中性的石墨层和克制的青绿色操作色，因此紧迫感来自状态、证据和层级，而不是装饰性光晕。

设计旋钮：`DESIGN_VARIANCE: 3`、`MOTION_INTENSITY: 2`、`VISUAL_DENSITY: 8`。这是运营产品，而非营销画布。

## 2. 色彩

### 调色板

| 角色 | Token | 值 | 用途 |
|---|---|---:|---|
| 画布 | `--surface-canvas` | `#0B0D0F` | 应用背景 |
| 外壳 | `--surface-shell` | `#111518` | 持久导航和顶栏 |
| 表面 | `--surface-default` | `#171C20` | 列表、面板、输入框 |
| 抬升表面 | `--surface-raised` | `#20272C` | 菜单、工作表、聚焦工作区 |
| 下沉表面 | `--surface-sunken` | `#080A0B` | 时间线、代码、追踪区域 |
| 主要文本 | `--text-primary` | `#F2F5F6` | 主要内容 |
| 次要文本 | `--text-secondary` | `#B8C1C7` | 辅助信息 |
| 弱化文本 | `--text-muted` | `#7D8A92` | 元数据和非活动控件 |
| 边框 | `--border-default` | `#30393F` | 结构分隔 |
| 细边框 | `--border-subtle` | `#222A2F` | 列表分隔线 |
| 强调色 | `--accent-primary` | `#1D9A8A` | 一个主要命令、链接、选中状态 |
| 强调色悬停 | `--accent-hover` | `#27B29F` | 悬停和按下反馈 |
| 信息 | `--status-info` | `#48A9E6` | 队列、同步和信息状态 |
| 成功 | `--status-success` | `#49B675` | 已验证或已完成状态 |
| 警告 | `--status-warning` | `#E6A23C` | 预算、降级或需注意状态 |
| 危险 | `--status-danger` | `#E76F6F` | 失败、破坏性操作或 R3 风险 |
| 焦点 | `--focus-ring` | `#9DDBFF` | 2px 可见焦点轮廓加 2px 偏移 |

### 规则

- `--accent-primary` 用于用户发起的主要命令、选中的导航和直接链接。它不是环境背景效果。
- 状态始终将颜色与标签以及图标或形状配对。单独的绿色圆点永远不表示“安全”或“完成”。
- 风险不是状态：即使操作本身成功，R3 徽章仍使用标签和图标。
- 文本与组件的配对必须符合 WCAG 2.2 AA：普通文本对比度 4.5:1，大号文本和非文本焦点指示器对比度 3:1。
- 任何组件都不得引入原始颜色。应先添加语义 token。

## 3. 排版

### 字体规格

| 角色 | 大小 | 字重 | 行高 | 字距 | 用途 |
|---|---:|---:|---:|---:|---|
| 页面标题 | 28px | 600 | 36px | 0 | 页面上下文 |
| 区域标题 | 20px | 600 | 28px | 0 | 主要面板标题 |
| 面板标题 | 16px | 600 | 24px | 0 | 卡片或检查器标题 |
| 正文 | 14px | 400 | 22px | 0 | 默认的密集阅读文本 |
| 标签 | 13px | 500 | 18px | 0 | 控件和列表标签 |
| 元数据 | 12px | 400 | 16px | 0 | 时间戳、范围、ID |
| 数据 | 12px | 400 | 18px | 0 | 哈希、成本、ID、日志 |

### 字体栈

- UI：`Fira Sans, Noto Sans SC, system-ui, -apple-system, BlinkMacSystemFont, sans-serif`
- 数据：`Fira Code, SFMono-Regular, Consolas, monospace`

### 规则

- 正文不得低于 14px；元数据至少为 12px，行高为 16px。
- 时长、成本、计数、序列、时间戳和哈希片段使用等宽数字。
- 不使用随视口缩放的字号或负字距。文本应在截断前换行，除非列表单元格明确提供可访问的完整值入口。
- 详情视图中的长 ID 和不间断值使用 `overflow-wrap: anywhere`；紧凑列表单元格截断时提供工具提示和复制操作。

## 4. 间距与布局

### 基础单位

所有意图间距以 4px 为基础。

| Token | 值 | 用途 |
|---|---:|---|
| `--space-1` | 4px | 图标与标签之间及徽章内部 |
| `--space-2` | 8px | 密集行间距 |
| `--space-3` | 12px | 表单字段内部和面板堆叠 |
| `--space-4` | 16px | 默认面板内边距 |
| `--space-5` | 20px | 页面区域节奏 |
| `--space-6` | 24px | 舒适的检查器间距 |
| `--space-8` | 32px | 页面区域分隔 |

### 应用网格

- 宽屏（`>=1440px`）：232px 导航、56px 顶栏、流式主内容区，可选 336px 上下文栏。
- 桌面（`1024px-1439px`）：216px 导航、流式主内容区，上下文栏以可调整大小的抽屉打开。
- 平板（`768px-1023px`）：64px 图标栏加带标签的导航抽屉；一个内容窗格和一个覆盖式检查器。
- 移动端（`<768px`）：一个主窗格、56px 顶栏、固定的五项底部导航，以及模态框/操作表形式的次要控件。
- 每个全高应用外壳都使用 `100dvb` 和有边界的 `scroll-body-shell`。主内容是页面滚动所有者；列表-详情页面可以为每个独立窗格指定一个滚动所有者。任何额外滚动区域都必须有文档化的职责。
- 数据网格使用内在轨道：`repeat(auto-fit, minmax(min(16rem, 100%), 1fr))`。在 375px 宽度下，主要内容不得要求水平滚动。

## 5. 组件

### AppShell

- **结构：**跳过链接、持久导航、顶栏、路由主体、上下文检查器或抽屉、移动端底部导航。
- **变体：**宽屏、桌面、平板、移动端；检查器打开/关闭。
- **状态：**已连接、重新连接中、离线、降级、权限过滤。
- **无障碍：**跳过链接指向路由标题；导航使用 `aria-current`；成功变更后播报当前范围。
- **布局：**`fixed-sidenav-shell`；只有路由主体拥有主滚动。

### ScopeSwitcher

- **结构：**工作区按钮、层级面包屑、可搜索选择器、当前策略/读写徽章。
- **变体：**完整面包屑、紧凑面包屑、移动端工作表。
- **状态：**加载中、已选择、只读、拒绝、选择已过期。
- **无障碍：**组合框语义、方向键导航、选择播报，并且不得静默重置筛选器。
- **动效：**180ms 不透明度/变换菜单过渡；减少动效时禁用。

### CommandPalette And QuickComposer

- **结构：**意图文本框、已附加上下文芯片、Agent/模型/工具策略摘要、提交命令、渐进式高级控件。
- **变体：**全局命令面板、Cowork 编写器、创建 Goal/Ticket 工作表。
- **状态：**空闲、验证中、提交中、已接受、权限拒绝、冲突、离线草稿。
- **无障碍：**可见标签；使用 `aria-describedby` 说明策略后果；在礼貌的实时区域播报提交状态。
- **规则：**已接受的命令显示 `Queued` 或 `Starting`，绝不显示虚假的“已完成”状态。

### StatusBadge And EvidenceChip

- **结构：**图标、可读标签、可选计数或时间戳；证据芯片还深链接到源对象。
- **变体：**运行状态、R0-R3 风险、信任、同步、外发、评判、审核、工件可见性。
- **状态：**当前、过期、不可用、已编辑隐藏。
- **无障碍：**颜色仅作补充；仅图标的紧凑形式具有可访问名称。

### AttentionRow And RunRow

- **结构：**前置严重性/风险标记、标题、范围、当前阶段、已用时间/预算数据、下一项安全操作、证据链接。
- **变体：**需注意、活动、暂停、阻塞、终止、部分成功。
- **状态：**加载骨架、空状态操作、数据不可访问、投影过期。
- **布局：**宽屏下一行显示；窄屏时换行为标签/数据/操作组，操作不移到行外。

### EvidenceInspector

- **结构：**摘要、溯源链接、不可变 ID、来源回执、模型/运行时数据、审计事件、恢复操作。
- **变体：**运行、审核、工件、记忆、外发。
- **状态：**加载中、已编辑隐藏、来源缺失、批准已过期、需要对账。
- **无障碍：**标签页使用 ARIA tabs 模式；表格公开表头关联并提供可复制值。

### ReviewDecisionSheet

- **结构：**风险摘要、确切载荷哈希、目标资源、预期外发、可逆性、证据清单、原因字段、操作按钮。
- **变体：**R1 快速确认、R2 审核、R3 高风险审核。
- **状态：**证据不可用、哈希不匹配、过期、角色不足、离线、已提交、已执行回执。
- **无障碍：**仅在打开时锁定焦点，将焦点恢复到触发器，要求明确的确认操作，并将校验错误与其输入关联。

### ArtifactPreview

- **结构：**版本标题、内容渲染器、来源摘要、编辑器或只读模式、Preview/Diff/Source/Receipt 标签页。
- **变体：**Markdown、代码、HTML、图像、PDF、Office、JSON、CSV、日志、不支持。
- **状态：**加载中、过大、不支持、渲染器失败、已编辑隐藏、编辑冲突已过期。
- **规则：**预览解析固定的工件版本。编辑会创建新版本，并使绑定到旧载荷的任何批准失效。

### StatePlane

- **结构：**状态图标、事实性标题、影响说明、下一项允许的操作、支持证据链接。
- **变体：**加载中、空、离线、错误、权限拒绝、冲突、部分成功、副作用未知。
- **无障碍：**错误/权限条件在适当时使用 `role="alert"`；成功使用 `aria-live="polite"`，绝不抢夺焦点。

### 基础组件展示门禁（Primitive Showcase Gate）

在编排 C10 产品屏幕之前，构建 `/design-system` 路由，在 375px、768px、1024px 和 1440px 下展示上述每个基础组件的默认、悬停、活动、焦点、禁用、加载中、空、错误、已编辑隐藏和长内容状态。使用 C10 工件捕获视觉和无障碍证据；在底层基础组件通过此门禁之前，任何产品屏幕都不被视为视觉完整。

## 6. 动效与交互

| Token | 时长 | 缓动 | 用途 |
|---|---:|---|---|
| `--motion-fast` | 120ms | ease-out | 按下和状态强调 |
| `--motion-standard` | 180ms | ease-out | 菜单、抽屉、检查器过渡 |
| `--motion-slow` | 240ms | ease-in-out | 路由级窗格替换 |

- 仅为 `opacity` 和 `transform` 添加动画；不要为布局尺寸或进度添加装饰性动效。
- 运行中的 Run 可以更新其时间线，而不会将读者从当前事件移开。如果读者已滚离尾部，新事件以不打扰的指示器追加。
- 每项操作都有悬停、按下、键盘焦点、禁用和进行中反馈。在触摸设备上，反馈由按压触发，绝不依赖悬停。
- `prefers-reduced-motion` 会禁用非必要过渡和事件进入动画。

## 7. 深度与表面

### 策略

使用**色调偏移加边框**。石墨亮度阶梯使外壳、工作表面、证据检查器和模态层级清晰可读，无需装饰性玻璃或发光渐变。

| 层级 | 处理方式 | 用途 |
|---|---|---|
| 外壳 | `--surface-shell` 加 `--border-subtle` | 导航和顶栏 |
| 默认 | `--surface-default` 加 1px `--border-default` | 列表、面板、字段 |
| 抬升 | `--surface-raised` 加 1px `--border-default` | 上下文菜单、检查器、覆盖层 |
| 模态 | 抬升表面加克制阴影 `0 16px 40px rgba(0,0,0,.32)` | 仅决策工作表 |

- 圆角半径：紧凑控件 4px，标准控件 6px，面板/工作表 8px。卡片不得超过 8px。
- 避免卡片嵌套卡片。列表和页面区域使用分隔线及留白；卡片独立框定可操作的工作项。

## 8. 无障碍约束与已接受的设计债务

### 约束

- 目标为 WCAG 2.2 AA，包括普通文本 4.5:1 对比度、可见的 2px 焦点处理、语义标题和合乎逻辑的键盘顺序。
- 交互触摸目标至少为 44×44 CSS 像素，与相邻触摸目标至少间隔 8px。
- 一致使用 Lucide 图标；每个仅图标控件都有可见工具提示和可访问名称。
- 键盘路径：`Tab` 遵循视觉顺序；Escape 关闭工作表/菜单；方向键操作列表框、树、标签页和画布菜单；拖动操作提供菜单和键盘替代方案。
- 审核操作必须无需指针拖动、颜色感知或动画即可完成。
- 尊重用户文本缩放，避免 375px 下主要内容水平滚动。

### 已接受的设计债务

| 项目 | 位置 | 接受原因 | 负责人 / 退出条件 |
|---|---|---|---|
| 工作流图画布编写 | 工作流表面 | 当前 C10-C12 计划尚未包含成熟的图编辑引擎或完整键盘模型。 | 先交付只读图和节点检查器；只有在图编辑键盘、缩放和屏幕阅读器设计经过测试后才清除债务。 |
| 移动端长富媒体差异 | 审核和工件表面 | 手机无法安全呈现完整的多列差异。 | 移动端打开证据摘要，并将确切深链接转移到桌面端；仅在紧凑证据清单完整时保留 R3 决策。 |
